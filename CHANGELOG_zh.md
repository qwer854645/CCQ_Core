# CCQ Core 0.4.54

相对 **0.4.50** 的更新。当前版本：`0.4.54`。

## 变更

- **吸管雪人冷却室（液体）**  
  罐内先通入水再通入冷却液时，可正确升级为冷冻，而不再一直停在冷却。更高档流体可顶掉罐内低档液体；罐内档次高于当前燃料时立即切换。普通 / 特殊冷却液标签补上流动态。

- **Create: Fluid — 流体接口 / 智能流体接口**  
  粘在矿车等动态结构上再拆装时，不再因放置顺序触发 `updateShape → 空气` 而静默消失；改为延迟一 tick 检查，仍无法附着时再破坏并掉落。

- **Create: Fluid — 矿车装配崩溃（Create 6.0.10）**  
  Fluid 1.2.4 仍访问已移除的 `Contraption.presentBlockEntities`，结构含排水口 / 铜水槽时装配或加载会踢出客户端。改为通过公开 API / 反射取方块实体，并修正铜水槽 mixin 未成功注入的问题。
